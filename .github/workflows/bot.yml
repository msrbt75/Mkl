name: Telegram Bot

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"  # يعيد التشغيل كل 6 ساعات تلقائياً

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
    - name: ✅ تحميل الملفات
      uses: actions/checkout@v3

    - name: 🧰 تثبيت Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 تثبيت المتطلبات
      run: pip install -r requirements.txt

    - name: ▶️ تشغيل البوت
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
      run: |
        echo "✅ بدء تشغيل البوت..."
        python yuome.py
